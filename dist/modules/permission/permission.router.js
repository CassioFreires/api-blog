"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const express_1 = require("express");
const permission_controller_1 = require("./permission.controller");
const permission_service_1 = require("./permission.service");
const permissionRouters = (0, express_1.Router)();
const permissioService = new permission_service_1.PermissionService();
const permissionController = new permission_controller_1.PermissionController(permissioService);
permissionRouters.post('/', ((req, res) => { permissionController.create(req, res); }));
permissionRouters.get('/', ((req, res) => { permissionController.getAll(req, res); }));
permissionRouters.get('/:id', ((req, res) => { permissionController.getById(req, res); }));
permissionRouters.get('/find-many', ((req, res) => { permissionController.getByIds(req, res); }));
permissionRouters.patch('/:id', ((req, res) => { permissionController.update(req, res); }));
permissionRouters.delete('/:id', ((req, res) => { permissionController.delete(req, res); }));
exports.default = permissionRouters;
